# 前期准备

为了实现该游戏项目，我们做了多方面准备。

## 游戏主题、玩法确定

该游戏为刘恩恺与李嘉润brain storming构思出来的一个游戏，起初准备设计为割草游戏，但经商榷后，考虑技术难度，还是决定制作一个3D塔防游戏。

游戏背景设置为一个末日情景下，幸存者防守丧尸等的进攻，争取生存下去的机会。

---

## 建模、动画准备

要实现3D游戏，建模是跑不开的事。李某某考虑过直接调用现有库的函数，生成一些3D结构来代替各种人物的建模，但是随便用ai vibe coding了一个demo，感觉效果过差，还是决定搞实在的3D建模。又因为不会自己建模，所所以获取建模的也很曲折。

### 建模工具选择、成功获取建模

在网上尝试了各种工具，实测下来，发现还是Meshy和tripo3D生成的建模最可靠（过来人的泪水）。我们创建了多个小号，蹭了每个号的免费额度，成功生成了我们所需要的所有建模，精度都很高（但这也为后面的bug埋伏笔了）。

### 获取建模动画

其实Meshy和tripo3D都有为建模绑定骨骼、绑定动画的功能，但是Meshy要氪金（我恨有钱人），tripo3D在后面的实测中，动画失效，所以开发者在这里选择了Mixamo（一个免费绑定建模骨骼、获取动画的web）。在Mixamo，经过了多次尝试，终于获取了所有所需的动画。

自此，建模与动画我们都成功获取了

---

## UI设想与获取

UI贴图我们考虑过自己做，但是技术过菜，最后还是选择去网上寻找UI包。辗转多个web，我们在Kenney网站获取了最终使用的UI包，包含了最后实现的界面贴图、经验贴图、血条等。

---

## 音乐、音效获取

由刘恩恺负责，在多个网站上创建n个小号，成功获取了多段mp3素材

---

# 开发过程

## 初次尝试（Unity + C#）

C#（C Sharp）是微软开发的面向对象语言，语法类似 Java/C++。游戏开发中最常见的用途是Unity引擎的脚本语言。

当前业内主流使用技术栈的就有Unity + C#，所以我们第一条想到的实现游戏的路线就是这条。但是，由于系统生态/配置的问题，在开发者的电脑上始终安装不了Unity Engine，所以我们甚至还没开始写代码就放弃了这条路线。（不过这条技术栈还是很值得学习的）

## 第二次尝试（C++ + raylib + assimp）

assimp（Open Asset Import Library）是一个开源的 3D模型导入库，支持40+种格式（glb、fbx、obj 等）

在这条路线路线里，我们基本完成了所有代码实现，但是始终有一个bug，就是模型贴图始终是花的，而且动画也不能正常播放（播放动画时整个建模直接消失），用ai审查了多遍也没发现问题，担心是建模与动画文件与代码不适配，我们甚至使用了建模转换其他类型的工具，也没能正常导入建模和播放动画。最终发现是由于建模与动画的精度太高（面数太高，超过了raylib支持的上限），从而导致导入异常。在这条路线里，这是致命问题，于是我们忍痛放弃了这条路线。

## 第三次尝试（GDScript + Godot Engine）

GDScript是Godot引擎的内置脚本语言，专门为游戏开发设计，语法类似于Python。

Godot是一个免费开源的跨平台游戏引擎，类似Unity/Unreal，但更轻量。

为什么走这条路线？它原生支持glTF/PBR材质。在初期实践中，我们很顺利的完成了场景搭建，正当我们以为能顺利实现所有功能时，发现无论如何，动画始终不能正常播放，只有动画开始时的第一帧模型。AnimationPlayer 在运行时实例化后骨骼路径断裂——编辑器里动画正常播放，代码里不动。加上编辑器的UID/tscn格式导致所有操作必须手动在GUI中完成，AI无法辅助。于是开发者愤怒放弃了这条路线TwT。

## 最终尝试（TypeScript + Three.js + GLTFLoader + Vite + HTML/CSS + Web Audio API + Electron）

这条路线我们主要借助ai Vibe coding实现，最终实现涉及到很多技术工具。

TypreScript：JavaScript 的超集。最大的价值是编译时类型检查——写错变量名、传错参数立即报错，不会等到运行时才发现。我们 12 个模块 1400 行代码，靠类型系统避免了大量低级 bug。

Three.js：浏览器 3D 框架，把 WebGL（OpenGL ES 的浏览器版）封装成直观的 API。

GLTFloader：Three.js 的 glTF 格式加载器，基于 Khronos Group 的官方规范。能正确读取 PBR 材质贴图（颜色/金属度/粗糙度/法线等）、骨骼绑定数据、动画关键帧。解决了 raylib 的 u16 索引溢出和 Godot 的骨骼路径断裂问题。

Vite：新一代前端构建工具。相比 Webpack，开发服务器启动 < 1 秒，改代码热更新 < 50ms。我们的 vite.config.ts 只用了 5 行配置，publicDir 指向 assets/ 让模型文件可以直接 HTTP 访问。

HTML+CSS：Three.js 画布铺满窗口，HTML 元素叠加在上方。我们手工组装 DOM 元素做按钮/面板/血条——结构简单、布局灵活、不需要额外 UI 框架。Kenney UI Pack 的 PNG 图片作为按钮/面板的背景纹理使用。

Web Audio API：浏览器原生音频 API——从内存的 AudioBuffer 创建音源 → 连接 GainNode 控制音量 → 输出到扬声器。我们预加载 8 个 MP3 并解码缓存，后续 playSound() 零延迟。

Electron：Chromium 浏览器内核 + Node.js 打包成的桌面应用框架。双击 .exe 打开窗口，自动加载 Vite 构建后的 HTML。本质就是用浏览器技术做桌面软件——VSCode 和 Discord 桌面版也是这么做的。

以上为技术工具简要介绍。在这条路线里，我们的游戏全程可依赖ai工具实现，因此开发者只需要规划大纲，让ai完成主体部分，剩下由人工细化优化。我们在过程中仍然遇见过动画没有正常播放的问题，但是经ai诊断后是骨骼数据损坏，于是我们上Mixamo重新绑定了动画，导出为fbx类型文件，最终成功实现了动画。至此难点全部被攻破，最后成功实现了这个Web-based 3D Game。

### 为什么选择这个技术栈？

相比前三条路线，这条技术栈有不可替代的优势：

**1. 全代码驱动，零编辑器依赖**

Godot 的失败让我们学到教训：凡是需要"手动在编辑器中操作"的引擎，AI 都无法辅助，开发速度被严重拖慢。Three.js 的场景、模型、动画、UI 全部用代码创建——1400 行 TypeScript 覆盖了从天空盒到存档的全部逻辑。每一行代码都可以被 AI 理解、生成、修改。

**2. glTF 支持业界最佳**

GLTFLoader 是 Khronos Group 官方维护的参考实现，质量远超 raylib 内置的 cgltf 和 Godot 的导入器。它原生支持 PBR 材质、u32 索引（无顶点数上限）、骨骼动画轨道——我们的 16 万顶点 wall.glb 直接加载，Mixamo 的 40 骨骼动画无缝播放。这正是前两条路线失败的根本原因，也是这条路线成功的基石。

**3. HTML/CSS 做 UI，灵活且免费**

Godot 的 Control 节点、raylib 的 raygui 各有学习成本。HTML/CSS 是每个开发者都熟悉的基础技术——我们用 Kenney UI Pack 的 PNG 做按钮背景，CSS 控制布局和交互，不引入任何额外框架。存档选择、关卡选择、暂停弹窗、购买面板、升级面板全部是纯 HTML 元素叠加在 3D 画布上方。

**4. Web 生态无依赖地狱**

C++/raylib 时代，assimp 的 6 个 DLL 依赖链让双击 .exe 启动就崩溃；Godot 的 .import 文件和 UID 系统让版本管理混乱。Three.js 只需 `npm install` 一个命令——所有依赖都在 node_modules 里，构建输出是纯静态文件，Vite 一行命令启动。

**5. Web Audio API 零成本音效**

不需要集成 FMOD、Wwise 等商业音频中间件。`AudioContext.decodeAudioData()` 解码 8 个 MP3 → `createBufferSource()` 播放 → `GainNode` 控制音量，30 行代码实现完整的音效 + 背景音乐系统。

**6. Electron 打包即桌面应用**

游戏在浏览器里运行（Web-based WebGL Game），套上 Electron 就变成双击启动的 .exe 桌面应用。VSCode、Discord、Figma 都用这个技术——成熟可靠，跨平台（Windows/Mac/Linux）。

**7. AI 友好度最高**

TypeScript 是 AI 训练数据最丰富的语言之一，Three.js 是 GitHub 上星标最多的 3D 框架。ChatGPT、Claude 等工具对这套技术栈的理解远超过 GDScript 或 raylib 的 C 代码。我们的开发模式——人规划、AI 执行、人验证——正是被这条技术栈的 AI 友好度所支撑的。

---

# 后期复盘

从最开始的各种素材收集，到过程实现，每一步都有很多难点。对创作3D游戏有兴趣的同学，强烈推荐学习使用Blender建模，虽然初期尝试一定没有Meshy或者Tripo3D等ai建模工具建的模好看，但是了解建模方法，是可以在ai建模的基础上添加自己的创意的，极大拓宽了建模最终能实现的广度；学习Unity或者Unreal等技术工具（当前3D游戏游戏主流技术工具），这俩都是强的，学了能实现更多自己的游戏，绝对不亏。